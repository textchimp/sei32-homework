//What number's bigger?
//Write an if statement that tests two number (numOne and numTwo), and prints the biggest number

let numOne = 10;
let numTwo = 20;

if (numOne > numTwo) {
console.log(${numOne} is the biggest number);
 } else {
 console.log(${numTwo} is the biggest number);
 };


//The Grade Assigner
//Write an if statement that gives users a score (A, B, C, D, or F) based on the test results

if (score => 90) {
  console.log('A');
} else if (score => 80){
  console.log('B');
} else if (score => 70) {
  console.log('C');
} else if (score => 60) {
  console.log('D');
} else (score < 50) {
  console.log('F');
}

score = 90;


//Part 1
const calculateSquare = function(anyNumber) {
  let square = anyNumber **2
  return resultSquare;
  console.log("The result of squaring ",anyNumber," is "resultSquare);
};

const halfNumber = function(anyNumber) {
  const divisionResult = anyNumber / 2);
  console.log("Half of", anyNumber, "is", divisionResult);
};

const percentOf = function(first, second) {
  let resultPercent = (first / second) * 100;
  console.log(`${first} is ${resultPercent}% of ${second}`);
};

const areaOfCircle = function(theRadius) {
  let resultCircle = Math.PI * (theRadius **2.);
  console.log(`The area of a circle with the radius of ${theRadius} is ${resultCircle.toFixed(3)}`);
};

//Part 2
